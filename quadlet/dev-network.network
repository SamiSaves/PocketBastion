[Network]
# Internal bridge network shared by all dev containers.
# Services bind to the WireGuard interface externally but communicate
# with each other via this network internally.
NetworkName=dev
Driver=bridge
Subnet=172.20.0.0/24
Gateway=172.20.0.1

Label=managed-by=quadlet
Label=project=opencode-server
